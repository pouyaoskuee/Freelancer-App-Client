import SendOtpForm from "../features/authentication/SendOTPForm.jsx";

function Auth() {
    return (
        <main className={'container'}>
            <SendOtpForm/>
        </main>
    );
}

export default Auth;